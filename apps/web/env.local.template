# Local development environment — copy this to .env.local (git-ignored)
# This overrides production defaults for local development only.
# .env.local is NEVER deployed to Vercel — it stays on your machine.

NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
