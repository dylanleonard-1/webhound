# provider-docs/github

**Provider role:** Source code hosting / OAuth provider / secret scanning.

**WebHound relevance:** GitHub OAuth integration (login); secret scanning patterns for detecting exposed credentials in repositories; webhook delivery format.

**Automation status:** AUTOMATION SUPPORTED — WebHound uses GitHub OAuth for user authentication.

**Source authority rule:** Tier-A **official GitHub docs only** for OAuth flows and secret scanning patterns.

**Do NOT mirror full provider docs** — link + short summary only (license/ToS).

**Content type:** link-summarize + authored detection notes.
