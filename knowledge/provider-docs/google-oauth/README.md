# provider-docs/google-oauth

**Provider role:** OAuth 2.0 / OpenID Connect identity provider.

**WebHound relevance:** Google OAuth integration for user authentication (Sign in with Google); scope configuration; redirect_uri security; token validation.

**Automation status:** AUTOMATION SUPPORTED — WebHound uses Google OAuth for user login.

**Source authority rule:** Tier-A **official Google Identity docs only** for OAuth flow details.

**Do NOT mirror full provider docs** — link + short summary only (license/ToS).

**Content type:** link-summarize + authored detection notes.
