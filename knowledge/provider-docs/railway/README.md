# provider-docs/railway

**Provider role:** PaaS hosting — WebHound API runs on Railway.

**WebHound relevance:** Scanner allowlisting (Railway has no edge WAF — no challenge pages); custom domain and TLS configuration; environment variable / secrets management; health check configuration.

**Automation status:** GUIDED — Railway does not provide automated scanner bypass; no edge WAF to configure.

**Source authority rule:** Tier-A **official Railway docs only** for deployment/networking configuration.

**Do NOT mirror full provider docs** — link + short summary only (license/ToS).

**Content type:** link-summarize + authored deployment notes.
