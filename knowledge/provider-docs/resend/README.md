# provider-docs/resend

**Provider role:** Transactional email delivery service — WebHound uses Resend for email.

**WebHound relevance:** SPF/DKIM/DMARC DNS record verification; domain setup for reliable email delivery; API key management; email deliverability best practices.

**Automation status:** GUIDED — DNS record setup is manual; Resend API used for email sends.

**Source authority rule:** Tier-A **official Resend docs only** for domain/DNS configuration.

**Do NOT mirror full provider docs** — link + short summary only (license/ToS).

**Content type:** link-summarize + authored DNS/deliverability notes.
