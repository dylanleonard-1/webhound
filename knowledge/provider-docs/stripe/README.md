# provider-docs/stripe

**Provider role:** Payment processing — WebHound uses Stripe for subscriptions/payments.

**WebHound relevance:** Webhook signature validation (Stripe-Signature header, HMAC-SHA256); webhook endpoint security; payment intent flows; checkout session security.

**Automation status:** GUIDED — Stripe webhook signing uses a shared secret configured via dashboard.

**Source authority rule:** Tier-A **official Stripe docs only** for webhook security implementation.

**Do NOT mirror full provider docs** — link + short summary only (license/ToS).

**Content type:** link-summarize + authored webhook security notes.
