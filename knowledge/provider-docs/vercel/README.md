# provider-docs/vercel

**Provider role:** Hosting / edge / firewall.

**WebHound relevance:** scanner allowlisting / trusted access + remediation guidance for sites fronted by this provider. Provider behavior ground truth = `apps/api/services/provider_access_registry.py`.

**Automation status:** GUIDED / MANUAL is the current default (IP System Bypass + Protection-Bypass are manual dashboard steps). **No full-automation claim.** Official docs required for deployment-protection / trusted-IPs.

**Source authority rule:** Tier-A **official provider docs only** for remediation/allowlisting steps. Community write-ups never override official docs.

**Do NOT mirror full provider docs** — link + short summary only (license/ToS). No pasted vendor pages.

**Future playbook location:** [`knowledge/playbooks/provider-access/`](../../playbooks/provider-access/README.md).

**Review status:** future-placeholder (link+summarize; populate in a later phase).

**Content type:** link-summarize.
